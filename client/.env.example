# Backend API (Express). No trailing slash.
# Local: http://localhost:3000
# Production (e.g. UI on Vercel, API on Railway): https://your-api.up.railway.app
# Must use https:// when the site is served over HTTPS — http:// is blocked on mobile Safari (mixed content).
VITE_API_URL=http://localhost:3000
